State the root-cause hypothesis for the candidate patch.

Return:
- A one-sentence summary.
- An executable Python function named `covers(value) -> bool`.
- A short rationale.

The predicate must describe the class of inputs the bug should affect,
not just the single failing input.
