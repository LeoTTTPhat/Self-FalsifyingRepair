[project]
name = "self-falsifying-repair"
version = "0.1.0"
description = "Research artifact scaffold for self-falsifying repair smoke tests."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
